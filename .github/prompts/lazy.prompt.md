---
description: Find the laziest correct solution via the restraint ladder — code first, minimal explanation.
agent: agent
argument-hint: <task>
---

Use the `ponytail` skill to solve: ${input}

Apply the restraint ladder and find the laziest correct solution. Use ${selection} / ${file} as context where relevant.

Output: the code first, with minimal explanation.
